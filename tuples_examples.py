
#tuples are the like lists, but with elements can't be changed (like immutable lists)
pi_tuple = (3, 2, 3, 5, 68, 9)
print(pi_tuple)

#convert tuple to list
list_from_tuple = list(pi_tuple)

#convert list to tuple
tuple_from_list = tuple(list_from_tuple)